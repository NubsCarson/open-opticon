bin/
sim/bin/
src/tamper/bin/
# ...but zk/host/src/bin is Rust source, not build output.
!zk/host/src/bin/
!zk/host/src/bin/**
test/fixtures/*.pcm
test/fixtures/*.pgm
*.o
*.out
bundle.json
dist/
he-log.json
__pycache__/
*.pyc

# stray cmd binaries if `go build ./cmd/<name>` is run from src/verifier without
# -o (they land as src/verifier/<name>); no tracked source is named he-* here.
src/verifier/he-*
